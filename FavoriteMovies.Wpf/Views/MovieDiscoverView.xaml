<UserControl x:Class="FavoriteMovies.Wpf.Views.MovieDiscoverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FavoriteMovies.Wpf.Views"
             mc:Ignorable="d" 
             FontSize="14"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Width="300" Margin="10" Padding="5" Text="{Binding Text,
                TargetNullValue='Discover your taste', FallbackValue='Discover your taste'}"/>
            <Button Content="Search" Margin="0 10 10 10" Padding="5"
                    Command="{Binding SearchMovieCommand}"/>
        </StackPanel>

        <ListBox Grid.Row="1" Margin="10" ItemsSource="{Binding Movies}">
           <ListBox.ItemsPanel>
               <ItemsPanelTemplate>
                   <WrapPanel Orientation="Horizontal"/>
               </ItemsPanelTemplate>
           </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
